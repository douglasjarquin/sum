.sum/
.deps/
.local/
__pycache__/
*.py[cod]
.env
.env.*
!.env.example
.DS_Store
.mcp.json
.codex/config.toml
.cursor/mcp.json
opencode.json
*.tar.gz
*.zip
.agents/skills/herdr
.claude/skills/herdr

# codegraph index of a checkout (issue #36): a regenerable per-checkout cache, never committed.
.codegraph/

# Managed project clones (sumctl project enroll); project code is neither sum source nor runtime state.
/projects/

# Verification run records written by .agents/skills/verify (see VERIFY.md).
.artifacts/

# Codex omo ultrawork local state (never product).
.omo/
