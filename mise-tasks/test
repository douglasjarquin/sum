#!/usr/bin/env bash
#MISE description="Run offline Go CLI tests and remaining skill tests (no credentials or LLM calls)"
set -euo pipefail
cd "$(dirname "$0")/.."
(cd go && go test ./...)
python -m unittest discover -s tests -p 'test_*skill*.py' -v
