#!/usr/bin/env bash
#MISE description="Run offline behavior tests and Go Mesh command-contract tests (no credentials or LLM calls)"
set -euo pipefail
cd "$(dirname "$0")/.."
python -m unittest discover -s tests -p 'test_*.py' -v
(cd go && go test ./internal/mesh ./cmd/herdr-mesh)

