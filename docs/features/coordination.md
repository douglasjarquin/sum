# Coordination and delegation

Journeys the coordinator, workers, and developers take through `sumctl` and the Herdr bridge.

| ID | Scenario | Driver | Evidence |
| --- | --- | --- | --- |
| `roles.claim` | The first pane claims coordinator atomically; later panes become developers or workers | automated: `tests/test_core.py` | offline suite |
| `roles.skill-delivery` | Worker and reviewer context views expose their canonical Sum skill identities while preserving role-specific sections and projections | automated: `tests/test_context.py`, `tests/test_metadata.py` | offline suite |
| `dispatch.worktree` | Dispatch creates one task checkout and branch and never writes to the primary clone | automated: `tests/test_core.py`, `scripts/demo.py` | offline suite, demo |
| `returns.durable` | A worker question survives a busy or closed coordinator and stays listed until applied | automated: `tests/test_returns.py`, `scripts/demo.py` | offline suite, demo |
| `returns.inbox-attention` | Records-only `sumctl inbox` lists a task whose only open return is attention, with the recorded excerpt and source pointer | automated: `tests/test_hook.py` | offline suite |
| `returns.foreign-machine` | A task whose `machine` is not this host is ignored by local `hook_event` and omitted from parent catch-up, while a local `sumctl ask` still appears in records-only inbox | automated: `tests/test_hook.py`, `tests/test_returns.py` | offline suite |
| `fleet.mixed-version` | Twelve scripted workers stay usable across a staged update, failed activation and recovery, rolling refresh, and rollback without restarting workers or losing callback records | automated: `tests/test_fleet.py` | offline suite |
| `update.known-good-recovery` | Activation and rollback require approved compatible targets; failed or interrupted activation restores the exact known-good runtime, retains pending recovery when auditing fails, and preserves task records across ordinary and independent recovery | automated: `tests/test_update_recovery.py`, `tests/test_update_recovery_review.py` | offline suite |
| `cleanup.guarded` | Post-merge cleanup removes only an identity-verified workspace after its agent and checkout processes exit, then archives the record | automated: `tests/test_cleanup.py` | offline suite |
| `execution.reservations` | Verified stop releases a worker slot without losing obligations; resume preserves prior attempt evidence and rejects stale IDs or full capacity | automated: `tests/test_execution.py` | `lib/execution_reservations.py`, `lib/sumctl.py`, offline suite |
| `execution.conservative-state` | Legacy records remain held until explicit adoption, malformed records including optional attempt metadata refuse admission and release, and old status writes cannot erase held reservations | automated: `tests/test_execution.py`, `tests/test_execution_metadata.py` | offline suite |
| `execution.observation-races` | Concurrent resume starts one successor; a live observer or verifier launch cannot be parked, and a crashed observer permits explicit reinspection | automated: `tests/test_execution.py`, `tests/test_execution_races.py` | offline suite |
| `execution.services-cleanup` | Parking during service inspection prevents launch; completed cleanup records release of the exact worker attempt | automated: `tests/test_execution_services.py` | offline suite |
| `execution.cleanup-races` | Cleanup excludes in-flight worker launch and competing cleanup/reconciliation; intent excludes new launches, newer attempts defeat stale proof, and conclusive refusal permits further approved work | automated: `tests/test_execution_cleanup_races.py` | offline suite |
| `execution.independent-verifier` | Root verification reserves capacity before checkout creation and retains uncertain execution for explicit stop reconciliation | automated: `tests/test_root_verification.py` | offline suite |
| `repairs.allowance` | Corrective sends and resumes share two task iterations; exhaustion saves one distinct decision without another prompt or launch | automated: `tests/test_repairs.py` | `lib/repair_control.py`, `lib/sumctl.py`, offline suite |
| `repairs.approval` | Only explicit coordinator-recorded human grants extend the allowance; repeated grants are idempotent and ordinary answers grant nothing | automated: `tests/test_repairs.py` | offline suite |
| `repairs.concurrent` | Shared instruction keys send once and concurrent contenders cannot exceed the last iteration | automated: `tests/test_repairs_races.py` | offline suite |
| `repairs.persisted` | Malformed records refuse without mutation; candidate, callback, refresh, saved harness, update, and rollback changes retain accounting; uncertain or crashed delivery is not replayed | automated: `tests/test_repair_metadata.py`, `tests/test_repair_lifecycle.py` | offline suite |
| `projects.enroll` | A named repository is enrolled once under `projects/<owner>/<repo>` and dispatched by name | automated: `tests/test_projects.py` | offline suite |
| `live.herdr-smoke` | A named lab Herdr session runs a real dispatch, question, and refresh | manual: `mise run test-live` on a host with Herdr 0.9.0 | operator report |
| `live.harness-canary` | An authenticated harness worker reads its brief and calls `sumctl ask` and `sumctl report` | manual: `docs/ACCEPTANCE.md` section 2 | operator report |
| `performance.hot-path-baseline` | Opt-in local metrics and `mise run benchmark` measure production helper entrypoints in fake and isolated real-Herdr labs before any rewrite decision | automated: `tests/test_benchmark.py` | `lib/sum_measure.py`, `scripts/benchmark.py`, `scripts/benchmark_fixture.py`, `scripts/benchmark_real.py`, `scripts/benchmark_report.py`, `mise-tasks/benchmark`, and retained `benchmarks/issue-37/raw.json`, `benchmarks/issue-37/report.md`, `benchmarks/issue-37/real-herdr.log` |
| `performance.go-cobra-prototype` | A cgo-free compiled Cobra entrypoint keeps the frozen Python helper as an explicit compatibility boundary while measuring native version/help and representative fixture dispatch | automated: `go/internal/cli/root_test.go`, `go/cmd/sumctl-go/main_test.go` | `docs/go-helper-prototype.md`, `go/go.mod`, `go/go.sum`, `go/internal/cli/root.go`, `go/cmd/sumctl-go/main.go`, `scripts/benchmark_go.py`, and benchmark JSON |
| `packaging.native-companion` | Setup and immutable release staging build cgo-free native companions, install Go Mesh as the live `bin/herdr-mesh` server, record dependency provenance, and reuse installed artifacts without replacing them | automated: `tests/test_packaging.py`, `tests/test_native_packaging.py`, `tests/test_core.py`, `go/internal/mesh/service_test.go`, `go/cmd/herdr-mesh/main_test.go` | `docs/dependency-inventory.json`, `docs/DEPENDENCIES.md`, `docs/go-mesh.md`, `scripts/setup.py`, `lib/sumctl.py`, `mise.toml`, `bin/herdr-mesh`, `go/cmd/herdr-mesh/main.go`, `go/internal/mesh/`, and the staged release manifest |
| `update.prior-helper-overlay` | A helper from before the Go Mesh cutover can copy `patches/herdr-mesh` when it stages this tree, smoke launches `bin/herdr-mesh`, and that wrapper execs `herdr-mesh-go` when that is the staged name | automated: `tests/test_packaging.py` | `patches/herdr-mesh/`, `bin/herdr-mesh`, `scripts/mcp_smoke.mjs` |
| `skills.namespace` | Sum-owned skills use one canonical `sum-*` identity across source directories, frontmatter, projections, generated references, and immutable release compatibility paths; collisions are refused without replacing a working projection | automated: `tests/test_skill_namespace.py`, `tests/test_core.py` | offline suite |
| `grok-bot.static` | Source-controlled Sum and project Bot templates exist, name real helper commands, forbid coordinator-pane work, and CONTRIBUTING requires a Grok Bot impact check | automated: `tests/test_grok_bot_templates.py` | offline suite |
| `roles.coordinator-dispatch-only` | Coordinator contract and Sum Bot template forbid research, planning, investigation, and implementation in the coordinator pane; those requests are dispatched | automated: `tests/test_grok_bot_templates.py` | offline suite |
| `roles.terminology` | First-party operating files and generated brief/contract samples use the Group 1 dictionary; a content check rejects themed role titles | automated: `tests/test_terminology.py` | `docs/TERMINOLOGY.md`, `tests/test_terminology.py`, `lib/sumctl.py`, `go/internal/contextview/contextview.go`, offline suite |
| `live.grok-bot-canary` | An operator installs from the real native template, enrolls one scratch project, questions and answers by IDs, and runs distinct worker then coordinator verification | manual: `docs/ACCEPTANCE.md` section 13 | operator report |
| `quota.codex-remainder` | Codex quota uses Remainder through `sumctl quota --provider codex`. Other providers keep quota-axi. Missing Remainder refuses without falling back. | automated: `tests/test_quota.py` | offline suite |
| `live.codex-quota-canary` | An authenticated Codex account reports remaining, exhausted, or unavailable through Remainder | manual: `docs/ACCEPTANCE.md` section 14 | operator report |
| `live.remote-machine-canary` | A real SSH machine added with Herdr `machine add` does not block local returns. Sum does not enroll the host. | manual: `docs/ACCEPTANCE.md` section 15 | operator report |
