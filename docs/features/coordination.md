# Coordination and delegation

Journeys the coordinator, workers, and developers take through `sumctl` and the Herdr bridge.

| ID | Scenario | Driver | Evidence |
| --- | --- | --- | --- |
| `roles.claim` | The first pane claims coordinator atomically; later panes become developers or workers | automated: `go/internal/cli/cutover_test.go`, `go/internal/cli/demo_test.go` | offline suite |
| `roles.machine-identity` | Renaming the host keeps the coordinator role and its return routes without `--reclaim`, because every recorded endpoint's `machine` is a stable identity hashed from the OS machine ID (`/etc/machine-id`, or the macOS platform UUID), never the raw ID or the hostname. A different host stays other-machine even when it shares the hostname: its coordinator claim is refused and reclaim fails as other-machine, and so are a VM clone that regenerated its machine ID with `.sum/` copied, and the tasks of a records backup restored there | automated: `go/internal/cli/machine_identity_test.go`, `go/internal/machine/machine_test.go` | offline suite. The tests pin the machine ID and hostname in process, and each scenario fails when the identity is the raw hostname. Implementation: `go/internal/machine/machine.go`, `go/internal/store/context.go`, `go/internal/store/store.go`, `go/internal/roleinit/designated.go`, `go/internal/app/app.go`, `go/internal/bindcmd/bind.go`. |
| `roles.legacy-hostname-records` | Records written by the hostname-keyed release keep resolving with no manual step. A hostname value names this host when it is the current hostname or was recorded under this host's identity in `.sum/machine.json`, which each registration updates. That covers a mix of old names once the host has carried each one. The coordinator's record and session files keyed by an old hostname are re-keyed on the next `init`, and a delivery recorded under the old key still counts, so it is not sent again. A name recorded under another host's identity never resolves here, and neither does one this host has not been seen carrying | automated: `go/internal/cli/machine_identity_test.go`, `go/internal/store/registration_test.go`, `go/internal/machine/machine_test.go` | offline suite. A pre-upgrade hostname record in a backup restored on a different host that shares that hostname still resolves there, as it did before. A name the host carried only under the older release resolves after one `init` while the host carries it again (the recovery step in `docs/recovery.md`). Implementation: `go/internal/machine/machine.go`, `go/internal/store/store.go`, `go/internal/roleinit/designated.go`, `go/internal/returns/returns.go`, `go/internal/returns/pump.go`. |
| `roles.legacy-worker-role` | After the upgrade and a later rename, the worker pane of a task recorded under this host's old hostname is still that task's worker: `init` registers it as the worker, `answer` refuses to record a decision from it, and notes and `env discover` attribute its entries to the worker | automated: `go/internal/cli/machine_identity_test.go` | offline suite. Implementation: `go/internal/roleinit/roleinit.go`, `go/internal/ask/ask.go`, `go/internal/notes/notes.go`, `go/internal/environment/discover.go`, `go/internal/environment/write.go`, with endpoint equality in `go/internal/machine/machine.go`. |
| `roles.recorded-machine-identity` | Endpoint records this release writes carry the stable machine identity, not the hostname. That covers the dispatched task with its worker attempt owner and occupant, the root verifier's owner and occupant, a staged release's `staged_by`, and the backup manifest's `machine`, which gains a display-only `hostname` | automated: `go/internal/cli/dispatch_launch_test.go`, `go/internal/cli/verify_execute_test.go`, `go/internal/cli/fleet_test.go`, `go/internal/cli/machine_identity_test.go` | offline suite. The development pane record written by `dev prepare --pane` and the task machine written by `bind --worker-pane` use the same identity but are not asserted by a test. Implementation: `go/internal/prepare/prepare.go`, `go/internal/verifycmd/execute.go`, `go/internal/release/stage.go`, `go/internal/backup/backup.go`, `go/internal/devcmd/dev.go`, `go/internal/bindcmd/bind.go`. |
| `roles.pane-incarnation` | A pane ID Herdr restored or reused under a new server does not inherit the coordinator or worker role. `init` records the pane's `terminal_id`, native `agent_session`, and shell pid and start time, and judges every later `init` against them: `same`, `new-conversation` (same terminal), `handoff` (new terminal, same shell), `restored` (the recorded native conversation resumed), and `legacy-verified` (a pre-incarnation record whose pane shell predates it) keep the role and refresh the record. `replaced`, `unrecorded`, and `unobservable` give developer, with an outcome-specific recovery; an unprovable outcome leaves the registration untouched, and none changes the coordinator record or any task. `init --reclaim` accepts a recorded coordinator that is `pane_not_found` or `replaced`, and refuses one that is verified, unobservable, or unprovable. `prepare`, `dispatch`, and `bind --worker-pane` record the worker registration for the pane they bind, and a worker checkout's `init` is judged against it. `RequireCoordinator` commands refuse a caller whose occupant is not the recorded coordinator, and a verified restored or handed-off coordinator's command records its new terminal. A task's recorded worker pane whose occupant is not proven never claims an unowned coordinator role | automated: `go/internal/cli/incarnation_test.go`, `go/internal/incarnation/incarnation_test.go`, `go/internal/cli/verification_policy_test.go`, `go/internal/cli/machine_identity_test.go` | offline suite against the fake Herdr, which scripts `terminal_id`, shell pid, and native session per pane, and a fake `ps` for process start times. `go/internal/cli/incarnation_test.go` and `go/internal/cli/dispatch_launch_test.go` (the dispatched worker registration's terminal and shell) cover these. The CLI test restores the coordinator pane under a new terminal and shell. It fails on the base commit, where the restored pane inits as coordinator, and passes here. The owner-changed-during-reclaim refusal is not separately asserted. Implementation: `go/internal/incarnation/incarnation.go`, `go/internal/roleinit/designated.go`, `go/internal/roleinit/roleinit.go`, `go/internal/app/app.go`, `go/internal/prepare/prepare.go`, `go/internal/bindcmd/bind.go`, `go/internal/store/store.go`. |
| `roles.skill-delivery` | Worker and reviewer context views expose their canonical Sum skill identities while preserving role-specific sections and projections; a worker whose active revision pins its procedure is offered only those pinned rows, not the live runtime `sum-worker` copy beside them | automated: `go/internal/cli/context_test.go`, `go/internal/cli/metadata_snippet_test.go`, `go/internal/cli/brief_procedure_test.go` | offline suite |
| `roles.bootstrap` | `AGENTS.md` is a small role bootstrap: explicit `init`, routing to the core of the role `init` returns, a reread after context compaction, and the authority and destructive-operation rules every role shares. It carries no action or optional-feature procedure (evidence publication, pipeline, capacity, repair, cleanup, graph, update, hook, metadata), and every file it or a role core names exists and is tracked, so every release tree and task runtime carries it | automated: `tests/test_operating_files.py`, `go/internal/skilltest/bootstrap_test.go` | offline suite; static checks of the operating files, not proof that a model follows them |
| `roles.coordinator-core` | `init` names the returned role's core under `procedure` with its runtime path, size, and sha256: `COORDINATOR.md` for the coordinator, `skills/sum-develop/SKILL.md` for a developer, the pinned procedure rows for a worker. A runtime whose coordinator core is missing, empty, oversized, not a regular file, or mismatched in a release manifest grants and keeps no coordinator role (first claim, the owner's re-init, and reclaim are all refused before anything is recorded); a developer is told the file is unusable and keeps its role. Every coordinator contract revision embeds the core after `AGENTS.md`, records its hash, reports `COORDINATOR.md changed`, and is not staged without it | automated: `go/internal/cli/role_bootstrap_test.go`, `go/internal/roleinit/roleinit_test.go`, `go/internal/procedure/procedure_test.go`, `go/internal/cli/fleet_test.go` | offline suite against a disposable runtime copy and the fake Herdr. Implementation: `go/internal/roleinit/procedure.go`, `go/internal/roleinit/designated.go`, `go/internal/refreshcmd/refresh.go`. |
| `brief.procedure-split` | The worker procedure is one required core (`skills/sum-worker/SKILL.md`, which keeps every standing prohibition) and on-demand files for evidence capture, services, the code graph, and refresh under `skills/sum-worker/references/`, each pinned like the core and listed in the brief as `Read when <condition>`, never inlined or required. A runtime missing any of them refuses dispatch before any Herdr call. A revision pinned before the split keeps its single row and stays valid; moving it to the split is a verification-affecting revision the worker must be asked to adopt, and a full refresh asks only for required files | automated: `go/internal/procedure/procedure_test.go`, `go/internal/brief/brief_test.go`, `go/internal/cli/brief_procedure_test.go` | offline suite. Implementation: `go/internal/procedure/procedure.go`, `go/internal/refreshcmd/refresh.go`. |
| `brief.unified-renderer` | Initial and regenerated briefs come from one template: the same sections in the same order and identical text outside the revision and decisions sections, with the revision id, the change summary since the previous revision, and each decision state (open, answered, closed-unapplied, applied) as data. Bounded `context --role worker` is the first read in the return channel and `show` is for explicit inspection; every return-channel command survives shell parsing with a state home whose path has spaces | automated: `go/internal/brief/brief_test.go` | offline suite. Implementation: `go/internal/brief/render.go`, `go/internal/brief/brief.go`, `go/internal/brief/regenerate.go`. |
| `brief.procedure-resource` | A brief references the worker procedure instead of copying it: every new revision pins `skills/sum-worker/SKILL.md` and its on-demand files (`brief.procedure-split`) as write-once, content-addressed task resources (`procedure/sum-worker-<sha16>.md`, recorded as task-relative rows with size and sha256 in the revision policy) and names its absolute path and hash. Dispatch refuses before any Herdr call when the runtime's procedure is missing, empty, oversized, not a regular file, or unlisted or mismatched in a release manifest; `WriteInitial` and `brief regenerate` write nothing then. A missing or altered pinned file makes the revision unusable for `brief request`, `brief adopt`, refresh, and `start` or `execution resume`, is never overwritten, and is repaired by moving it aside and running `brief regenerate`. `start` prompts the active revision's file. `context --role worker` lists the pinned rows with `ok`; they stay readable after the runtime copy is removed, an update pins a new file, rollback reuses the earlier file by hash, and `backup` carries the pinned files | automated: `go/internal/procedure/procedure_test.go`, `go/internal/brief/brief_test.go`, `go/internal/cli/brief_procedure_test.go`, `go/internal/cli/fleet_test.go` | offline suite against a disposable runtime copy and the fake Herdr. A restore from backup into a new home is not separately exercised. Implementation: `go/internal/procedure/procedure.go`, `go/internal/versions/versions.go`, `go/internal/prepare/prepare.go`, `go/internal/backup/backup.go`, `go/internal/contextview/contextview.go`. |
| `brief.decision-only-refresh` | A requested revision whose policy (pinned procedure included), return commands, and approved fingerprint equal the task's active revision is delivered as decision-only: the refresh message and the worker return notice name `context TASK --role worker --section decisions` and the `brief adopt` command, and the full revision only as the record for a fresh session. A procedure change staged but never adopted keeps a later revision from qualifying, so its refresh names the full file. Return notices name `context TASK --role worker` or `--role coordinator` as the durable read | automated: `go/internal/cli/brief_procedure_test.go` | offline suite, fake Herdr, including a worker busy at `refresh request` that receives the decision-only wording from the next `pump`. Implementation: `go/internal/versions/versions.go`, `go/internal/refreshcmd/refresh.go`, `go/internal/returns/pump.go`. |
| `dispatch.worktree` | Dispatch creates one task checkout and branch and never writes to the primary clone | automated: `go/internal/cli/cutover_test.go`, `go/internal/cli/demo_test.go` | offline suite, demo |
| `dispatch.harness-argv` | A worker launch carries the harness's fixed argv: a devin worker starts past the first-run/trust gate, other harnesses get none, and a transient `agent_pane_busy` refusal retries within the launch bound | automated: `go/internal/cli/dispatch_launch_test.go` | offline suite. The real devin gate acceptance remains the operator's authenticated-harness canary. Implementation: `go/internal/settings/settings.go`, `go/internal/launch/launch.go`, `go/internal/prepare/prepare.go`. |
| `returns.durable` | A worker question survives a busy or closed coordinator and stays listed until applied, or until cleanup settles it once every attempt that could consume the answer has verified stop evidence | automated: `go/internal/cli/demo_test.go` | offline suite, demo. `go/internal/cleanup/apply_test.go` covers the settled transition under the task lock; a `settled` question reads like an applied one in `go/internal/archive/archive.go`, `go/internal/ask/ask.go`, `go/internal/statuscmd/statuscmd.go`, `go/internal/metadata/metadata.go`, and repair grant-decision matching in `go/internal/repair/repair.go` (covered by `go/internal/repair/repair_test.go` and the granted-settlement apply test in `go/internal/cleanup/apply_test.go`). |
| `returns.coordinator-close` | The coordinator closes an answered question whose worker can no longer apply it with `answer TASK QUESTION --close --reason TEXT`, after which `archive --acknowledge` succeeds. The question becomes `closed-unapplied`, never `applied`, and records `closed_at`, `closed_by` (role, machine, session, pane), `close_reason`, and the released `closed_attempt`; `resolve` cannot turn it into an application. It refuses from any pane but the registered coordinator, on an open question, and while the worker attempt is anything but `released` (held, starting, running, observing, uncertain, or a legacy task with no attempt), naming the `execution park` that proves the stop | automated: `go/internal/cli/answer_close_test.go` | offline suite; the test drives the archive path end to end. Not separately covered: a `closed-unapplied` question also reads like an applied one through `ask.Discharged` in `go/internal/archive/archive.go`, `go/internal/statuscmd/statuscmd.go`, `go/internal/metadata/metadata.go`, `go/internal/cleanup/inspect.go`, and `go/internal/contextview/contextview.go`, keeps a recorded repair grant valid in `go/internal/repair/repair.go`, is never listed as an unapplied answer owed to the worker in `go/internal/returns/returns.go`, and appears as closed in a regenerated brief (`go/internal/brief/regenerate.go`). Implementation: `go/internal/ask/ask.go`, `go/internal/cli/native.go`. |
| `returns.inbox-attention` | Records-only `sumctl inbox` lists a task whose only open return is attention, with the recorded excerpt and source pointer | automated: `go/internal/cli/hook_test.go` | offline suite |
| `returns.foreign-machine` | A task whose `machine` does not name this host is ignored by local `hook_event` and omitted from parent catch-up, while a local `sumctl ask` still appears in records-only inbox | automated: `go/internal/cli/hook_test.go`, `go/internal/cli/demo_test.go` | offline suite |
| `returns.cleanup-sweep` | The coordinator's explicit `sumctl sweep [--task ID]... [--budget SECONDS]` is the only batch maintenance: one `pr reconcile` observation per recorded open PR (which may publish evidence and pipeline blocks) and one guarded cleanup apply per task pending cleanup, tasks least recently maintained first (the older of the PR's last observation or failed attempt, `observe_failed_at`, and the cleanup record time), so a PR whose observation keeps failing moves behind PRs not yet tried, each task re-read and judged on the fresh record before acting, per-task failures named in their row instead of aborting the pass. After a `gh` timeout or unknown effect no further PR is observed in that pass and those tasks are `deferred` with `sumctl pr reconcile TASK`, while cleanup rows still run; no task starts after the budget (default 60 s, `--budget 0` starts nothing, every untouched task `deferred` with its next command), and a started task finishes under its helpers' own bounds. The output carries `rows`, `deferred`, `budget_ms`, `elapsed_ms`, and the `maintenance` view after the pass; a second sweep with nothing pending does nothing. It refuses outside the registered coordinator pane, and the developer guard treats it as a write | automated: `go/internal/lifecycle/lifecycle_test.go`, `go/internal/cli/boundary_test.go` | offline suite against lab Herdr sessions and a fake `gh`; the live-coordinator path stays `live.herdr-smoke`. The developer-checkout refusal against installation state follows from `sweep` being absent from `ReadOnlyCommands` in `go/internal/guard/guard.go` and is not separately asserted. Implementation: `go/internal/lifecycle/lifecycle.go`, invoked only from the `sweep` command in `go/internal/cli/native.go`. |
| `returns.pass-budget` | One delivery pass (`init`, `bind`, `pump`, `notice`, the single-task notice a task write triggers, `hook enable`, and hook events) has one overall budget (default 20 s, `pump --budget SECONDS`; one hook event shares one deadline across all its pumps). The recipient's delivery lock (see `returns.recipient-serialization`) is taken with a wait bounded by the remaining budget, and a busy lock defers; with the lock held each task is re-read and its route and open obligations recomputed, so an obligation closed or a route rebound since the pass read the task gets nothing (listed as `revalidated` drops). One `agent list` per Herdr session per pass selects recipients: snapshot-busy, absent, or wrong-cwd recipients are `not-delivered` without their own call, and a settled recipient is re-observed with `agent get` immediately before `agent prompt`. A Herdr call starts only when its own timeout (5 s observe, 5 s prompt) plus the runner's 2 s pipe grace still fits, so no started call is cut and no prompt turns `uncertain` because of the budget; a failed, timed-out, or unknown-effect call trips its session for the rest of the pass and that session's later recipients are `not-delivered` (session unavailable this pass), counting toward `stalled`. Recipients the budget or a busy lock did not reach are `deferred` with no delivery record and stay pending. Order is the caller's own inline listing first, then least recently attempted recipients, so a deferred recipient goes first next pass. The output adds `fanout` (sessions, Herdr calls, elapsed and budget milliseconds, deferred) whenever a recipient was visited | automated: `go/internal/returns/pass_test.go`, `go/internal/store/store_test.go`, `go/internal/herdrclient/snapshot_test.go` | offline suite against a fake Herdr runner. `go/internal/returns/pass_test.go` covers a busy session costing one observation, re-observation before the prompt, a hung observation or prompt tripping its session within the budget, a healthy session not starved by a hung one, a too-small budget deferring while the inline listing is still presented, fairness reaching a deferred recipient next, no prompt to a route rebound after the snapshot, no stamp on an obligation closed after the snapshot, and deferral while an older release's pass holds the compatibility lock exclusively; `go/internal/store/store_test.go` covers the lock wait giving up at the caller's deadline; `go/internal/herdrclient/snapshot_test.go` covers one list per session, a kept list failure, the caller's deadline, and a sticky trip. The one `git rev-parse` per pass for the runtime SHA is not separately asserted. Implementation: `go/internal/returns/pump.go`, `go/internal/herdrclient/snapshot.go`, `go/internal/herdrclient/herdrclient.go`, `go/internal/store/store.go`, `go/internal/hookstatus/event.go`. |
| `returns.recipient-serialization` | Delivery is serialized per recipient: two sum processes delivering to unrelated recipients do not wait for each other's Herdr observation or prompt, and two delivering to one recipient take turns, with at most one prompt per open return even when one task records the coordinator's machine under the stable ID and another under a legacy hostname alias. Each recipient's lock is keyed by its canonical endpoint (machine, session, pane) in a per-recipient lock file under the state home; a recipient another operation holds is revisited after every other recipient, waiting only while an observation and a prompt could still fit the budget, and otherwise `deferred`. The fixed, enforced lock order is `.deliver.lock` (shared by this release, exclusive in older releases, so mixed versions serialize completely), then one recipient lock, then the state lock, which is never held across a Herdr call; an out-of-order acquisition fails with a lock-order error instead of deadlocking. Right before the prompt, under the state lock, the returns are re-read: one that closed or was rebound while the recipient was observed is dropped and the notice rebuilt from the rest, nothing is sent when none remain, the prompt must still fit the budget, and the attempt is recorded `in-flight` before the prompt. A crash before that stamp leaves the return pending and the next pass sends it once; a crash after it leaves `uncertain`, never replayed and never cleared by age or process disappearance, and the dead process's locks are free. `repair send` takes the same locks for its worker before its duplicate-key and allowance checks. The pass output's `fanout` adds `lock_wait_ms` | automated: `go/internal/returns/concurrency_test.go`, `go/internal/store/store_test.go`, `go/internal/returns/pass_test.go`, `go/internal/cli/repair_test.go` | offline suite; the concurrency tests run the second operation as a separate OS process (the test binary re-executed) against one state home and a fake Herdr that can hold one pane's `agent get` or `agent prompt`. They cover an unrelated recipient submitted in about 0.1 s while another process's prompt is held (before this change it waited out its whole 8 s budget and was `deferred`), an older release's exclusive acquire refused while a new pass is mid-prompt, one coalesced prompt across a stable-ID and a legacy-hostname spelling of the coordinator, a contended recipient revisited last and found already sent, a contended recipient deferred at once when too little budget remains and deferred when held past its bound, a crash mid-prompt staying `uncertain`, a crash mid-observation leaving the return to be sent once, a close or rebind during observation sending nothing, a recipient deregistered during observation refused at the claim with no prompt, and a survivor sent alone after a coalesced return closed. `go/internal/cli/repair_test.go` covers `repair send` refusing with nothing recorded while a concurrent delivery holds the worker's lock (and sending with the same key afterwards), and refusing a worker rebound while it waited. `go/internal/store/store_test.go` covers every out-of-order acquisition refusing, shared holders coexisting, an exclusive try refused while one remains, and a held recipient giving up at the deadline. Concurrent prompts from two processes into one real Herdr session are not exercised (`live.herdr-smoke` does not cover them), and two concurrent `repair send` calls with one key are not separately asserted. `refresh`, `prepare`, and the Mesh relay prompt panes without the recipient lock, as they never took the delivery lock. Implementation: `go/internal/store/store.go`, `go/internal/returns/deliverylock.go`, `go/internal/returns/pump.go`, `go/internal/repair/repair.go`. |
| `returns.pane-incarnation` | A return is never submitted to a pane whose occupant is not the recorded recipient. The fresh `agent get` taken right before the prompt is judged inside the state-locked claim against the re-read worker registration or coordinator record; a shell probe it needs is taken before the lock. A refused recipient is a `refused` row: nothing sent, nothing recorded, the return still pending, and a `submitted` or `uncertain` delivery keeps its state. A restart between the pass's `agent list` and its `agent get`, or an incarnation recorded while the recipient was observed, sends nothing. A verified worker whose record is out of date (a native session reported since launch, a handoff terminal) has its registration refreshed by the claim, so a later native restore is still recognized. The caller's own inline listing is presented only to the recorded occupant; a caller verified earlier in the operation skips that check only for its own role and task. `repair send` refuses a replaced worker before anything is stamped or charged, refresh leaves a replaced target, or one whose record changed while it was observed (re-read under the state lock right before the prompt), `pending-unreachable` with nothing sent, and `execution park` keeps the reservation of an attempt whose restored pane holds a different live agent | automated: `go/internal/returns/incarnation_test.go`, `go/internal/cli/repair_test.go`, `go/internal/refreshcmd/occupant_test.go`, `go/internal/execution/park_test.go`, `go/internal/cli/incarnation_test.go` | offline suite; the rebind-during-observation case runs the pass as a separate OS process against a fake Herdr holding `agent get`. The replaced-worker case fails on the base commit, which prompts the restored pane once. A restart after the claim's observation and before `agent prompt` cannot be excluded: Herdr 0.9.0 has no prompt precondition (`docs/herdr-backend.md`). Implementation: `go/internal/returns/occupant.go`, `go/internal/returns/pump.go`, `go/internal/repair/repair.go`, `go/internal/refreshcmd/refresh.go`. |
| `coordination.fast-path-boundary` | Three command classes with one boundary. Read-only views (`status`, `inbox`, and their `--live` form) write nothing under the state home and carry a `maintenance` view built from saved records only: recorded open PRs with `task`, `pr`, `state`, and `observed_at` as recorded and `next: sumctl pr reconcile TASK`, cleanup pending, blocked, removing, or ready with its state, time, blockers, and `next: sumctl cleanup TASK`, `next: sumctl sweep` when anything is listed, and a note that nothing was re-observed. `--live` from a pane with a Herdr context adds one bounded `agent list` per Herdr session of the listed local, unarchived tasks, an `observed` state per row (the agent state, `absent` when missing from its session's list without its own lookup, or `unobserved` with a reason), `live: true`, and `fanout`; without a Herdr context it returns `live: false` with `live_reason` and the plain records view. It never delivers returns, creates attention records, reconciles a `removing` cleanup, or calls GitHub. Fast delivery (`init`, `pump`, `bind`, `notice`, task-write notices, `hook enable`, hook events) never runs `gh`, publishes evidence or pipeline blocks, or inspects or applies cleanup, so a merge is no longer observed on those paths; coordinator `init` and `pump` add the `maintenance` view. Only the explicit maintenance commands (`sweep`, `pr reconcile`, `pipeline`, `cleanup`) reach GitHub | automated: `go/internal/cli/boundary_test.go`, `go/internal/cli/status_test.go`, `go/internal/lifecycle/lifecycle_test.go` | offline suite with a fake `gh` that logs every call and fake Herdr. `go/internal/cli/boundary_test.go` runs coordinator `init`, `status`, `inbox`, both `--live` forms, `pump`, `bind --parent-only`, `hook enable`, and hook events with an open-PR task and a merged cleanup-pending task, asserting an unchanged state home across the read-only views, one Herdr call and an `observed` row under `--live`, both tasks named in `maintenance` with the recorded `observed_at`, an empty `gh` log across every fast path, the merged task neither cleaned up nor archived with its checkout still present, and the open PR's `observed_at` unchanged; then `sweep` makes the `gh` call and reports a `pr-observe` row for the open task and a `cleanup` row for the merged one. `go/internal/cli/status_test.go` covers `live: false` with a reason outside Herdr and pins the records view's stdout; `TestPending_listsMaintenanceFromRecords` covers the view itself. A `--live` row reading `absent` or `unobserved` is not separately asserted. Implementation: `go/internal/statuscmd/statuscmd.go`, `go/internal/lifecycle/lifecycle.go`, `go/internal/roleinit/designated.go`, `go/internal/cli/native.go`, `go/internal/bindcmd/bind.go`, `go/internal/hookstatus/enable.go`, `go/internal/hookstatus/event.go`, `go/internal/helpview/catalog.json`. `init` reads the tasks once and reuses that read for the capacity, hook (`go/internal/hookstatus/hookstatus.go`), and maintenance summaries. |
| `fleet.mixed-version` | Twelve scripted workers stay usable across a staged update, failed activation and recovery, rolling refresh, and rollback without restarting workers or losing callback records. A closed worker pane is `unreachable` for that refresh revision, not a looping worker notice. | automated: `go/internal/cli/fleet_test.go` | offline suite. Fake Herdr. Distinct pinned roles, not twelve copies of one dispatch. Rollback is proven on the pinned procedure file the rolled-back revision references, which reuses the update-one file by hash, while every earlier revision stays usable. Implementation: `go/internal/refreshcmd/refresh.go`, `go/internal/herdrclient/snapshot.go`, `go/internal/returns/returns.go`, `go/internal/returns/pump.go`, `go/internal/versions/versions.go`, `go/internal/procedure/procedure.go`. |
| `update.known-good-recovery` | Activation and rollback require approved compatible targets; failed or interrupted activation restores the exact known-good runtime, retains pending recovery when auditing fails, and preserves task records across ordinary and independent recovery | automated: `go/internal/updatecmd/activation_test.go`, `go/internal/updatecmd/rollback_test.go` | offline suite. Those tests cover approved compatible targets, failed and interrupted activation, independent recover, retained pending evidence, and preserved task records. |
| `update.stale-known-good-reconcile` | A verified serving default with stale committed known-good and empty pending is reconciled, then apply proceeds: a checkout already at origin/main activates, known-good becomes that SHA, a pending generation still blocks, and a dirty checkout still switches the runtime with `checkout-instructions` deferred. The recovery-history refusal does not fire after a clean git pull. | automated: `go/internal/updatecmd/activation_test.go`, `go/internal/updatecmd/update_test.go`, `go/internal/updatecmd/sync_test.go` | offline suite. `TestApply_reconcilesStaleKnownGoodWhenHeadAlreadyAtOrigin`, `TestApply_reconcilesStaleKnownGoodThenActivatesPulledSHA`, `TestApply_staleKnownGoodDirtyCheckoutStillActivatesRuntime`, `TestApply_staleKnownGoodInterruptRecoversServingDefaultNotStale`, `TestApply_refusesWhileActivationPending`, `TestApply_refusesDirtyCheckoutAndKeepsRuntime`. Implementation: `go/internal/updatecmd/activation.go`. |
| `update.pre-identity-guard` | Once the coordinator's `.sum/context.json`, a session registration, or a task carries a stable `m-` machine identity, `update apply --ref`, `update rollback` (default, `--to SHA`, and `--to checkout`), and `update recover` refuse a target that predates it, naming the demotion and other-machine reclaim refusal it would cause. A staged release predates it when its own tree lacks `go/internal/machine/machine.go` (#202's file, read from its verified `files` list, never from a field the staging runtime stamps); a checkout target predates it when its prebuilt `.local/bin/sumctl` does not report `supports.machine_identity` 1 in `release-contract`, whatever HEAD carries. `--allow-pre-machine-identity` selects it anyway and records `machine_identity_override` in `.local/updates.jsonl`. A target that offers the identity, including one whose manifest a pre-guard runtime such as 4eb8591 staged without any identity field, and records that carry only hostnames, are unaffected | automated: `go/internal/updatecmd/identity_test.go`, `go/internal/contract/contract_test.go` | offline suite. The lab trees commit or omit the marker, a checkout whose HEAD carries it but whose helper's contract lacks the field is refused, and one test classifies this repository's own 1f2806d, 4eb8591, and HEAD, so it needs full Git history. Compensation after a failed activation restores the runtime that served moments before without asking for the flag again, and records the override. Implementation: `go/internal/updatecmd/identity.go`, `go/internal/updatecmd/update.go`, `go/internal/updatecmd/activation.go`, `go/internal/contract/contract.go`, `go/internal/machine/machine.go`, `go/internal/cli/update.go`. |
| `update.runtime-revision` | `doctor` names the revision the active runtime was staged from and how far the installation checkout has moved past it; an ancestor runtime reports its distance and stays ok, while an unidentifiable or off-history runtime does not | automated: `go/internal/doctor/doctor_test.go` | offline suite. Covers no staged release, a runtime at HEAD, an ancestor runtime reporting distance, a runtime that is not on the checkout's history, a release tree whose manifest is missing or names no source revision, and a non-Git installation. Implementation: `go/internal/doctor/doctor.go`. |
| `cleanup.guarded` | Post-merge cleanup removes only an identity-verified workspace after its agent and checkout processes exit, then archives the record; a saved intent continues a matching interrupted apply or is re-derived from fresh inspection instead of refusing forever; processes provably bound to the checkout (cwd inside it, or argv naming it like `serve --mcp --path <checkout>`) that are not descendants of a still-live recorded pane or shell pid are named orphans once the pane is proven absent — even while its workspace record persists — stopped with one SIGTERM each and verified gone (never SIGKILL); the task's own reviewer shell is exempt as `cleanup.reviewer-shell` describes; ignored files block unless they are a known regenerable cache, which includes an ignored `.local/` holding only the `.local/bin/sumctl` that sum's own `mise run test`/`verify`/`demo` builds from the tracked `go/cmd/sumctl`, while any other file under `.local/` still blocks; and a closed-pane checkout that is still registered, on the recorded branch, and clean is adopted and removed by plain `git worktree remove` while the branch is kept | manual: identity-verified apply-and-archive against real Herdr is not yet automated | `go/internal/cleanup/apply_test.go` covers a matching intent continuing, a stale intent re-deriving, an orphan stopped with one SIGTERM and verified exit before adoption, an orphan stopped while its workspace is still present, an argv-only-bound daemon stopped, a bound descendant of a live recorded pid staying a named blocker, answered-question settlement after verified worker death, and closed-pane adoption, against fake Herdr and `tests/fixtures/lsof.py`. `go/internal/cleanup/inspect_test.go` covers the sum-built binary under `.local/` classified disposable and a foreign file, a symlinked binary, or a checkout without `go/cmd/sumctl` keeping `.local/` a blocker. `go/internal/cleanup/stop_test.go` covers stop-evidence refusal, a closed pane that is not an occupant, and a live checkout process that still blocks. `go/internal/cli/demo_test.go` never runs cleanup. Implementation: `go/internal/cleanup/inspect.go`, `go/internal/cleanup/run.go`, `go/internal/proc/proc.go`, `go/internal/proc/inspect.go`. |
| `cleanup.reviewer-shell` | The shell pid Herdr reports for the task's recorded reviewer pane is that pane's, closed through the reviewer path, and is neither a worker occupant nor an orphan, while anything it runs still blocks. This holds only for a reviewer pane on this machine, in this session, distinct from the worker pane, observed now with that pid in its foreground as a known shell and its live cwd the task checkout; otherwise the shell blocks like any other checkout process. The reviewer's recorded cwd is the installation root the review ran from and is not compared | automated: `go/internal/cleanup/stop_test.go`, `go/internal/execution/stop_test.go` | offline suite, with the reviewer recorded through `review.Run` or with the lab context's cwd as production records it. `go/internal/cleanup/stop_test.go` covers the reviewer shell in the checkout counted neither as a detached worker occupant nor as an orphan, including when the worker pane's process-info is unreadable; that shell's child and a stray process still blocking; and, with the worker pane gone, that shell's real child staying a named `occupant`/`execution` blocker rather than an orphan apply would terminate. `go/internal/execution/stop_test.go` covers the exemption for a reviewer recorded through `review.Run`, and keeps the shell blocking when the reviewer is on another machine or session, is the worker pane, is unobservable, has no readable process-info, is not an idle known shell, has a different shell in its foreground, has a live cwd other than the task checkout, or the task records no checkout. Implementation: `go/internal/execution/stop.go`, `go/internal/cleanup/inspect.go`. |
| `dispatch.launch-identity` | A launched worker attempt binds the pane's foreground process-group leader as its occupant even when the agent forks children into that group (a Claude worker's MCP servers), becomes `running`, and accepts an in-scope `repair send` once settled; a foreground whose leader is absent or is the shell stays `uncertain`, binds no pid, and `repair send` refuses it | automated: `go/internal/cli/dispatch_launch_test.go`, `go/internal/environment/start_test.go` | offline suite, against fake Herdr scenarios `FAKE_AGENT_CHILDREN` and `FAKE_AGENT_LEADER_GONE` in `tests/fixtures/herdr.py`, whose `process-info` shape (a leader plus its child sharing `foreground_process_group_id`) was observed from real Herdr 0.9.0 in a lab session. Attempts recorded `uncertain` before this rule are not re-bound. A real authenticated Claude launch remains the operator's canary. Implementation: `go/internal/environment/start.go`, `go/internal/prepare/prepare.go`. |
| `execution.reservations` | Verified stop releases a worker slot without losing obligations; resume preserves prior attempt evidence and rejects stale IDs or full capacity | automated: `go/internal/execution/park_test.go` | offline suite covers verified stop, retained obligations, stale park IDs, and a user-closed pane that parks. Resume preserving prior attempt evidence and full-capacity resume refusal is not yet automated. Implementation: `go/internal/reservations`, `go/internal/execution`. |
| `execution.stop-evidence` | Missing, stale, or unobservable process identity cannot release capacity; a user-closed pane with no checkout occupant is conclusive stop; parking and cleanup share one stopped-versus-unknown proof, including owned children after a dead parent, and a verified-dead worker pane also names surviving checkout-bound processes (cwd inside, or argv naming the checkout) as orphan evidence cleanup can stop explicitly; the observed shell pid of the task's recorded reviewer pane (this machine and session, not the worker pane, an idle known shell whose live cwd is the task checkout) is excluded from the worker's checkout-bound processes, its children are not | automated: `go/internal/execution/park_test.go`, `go/internal/execution/stop_test.go`, `go/internal/cleanup/stop_test.go` | offline suite. Implementation: `go/internal/execution/stop.go`, `go/internal/proc/inspect.go`, `go/internal/herdrclient/herdrclient.go`. |
| `execution.conservative-state` | Legacy records remain held until explicit adoption, malformed records including optional attempt metadata refuse admission and release, and old status writes cannot erase held reservations | manual: conservative reservation-state driver is not yet automated | `go/internal/cli/cutover_test.go` does not exercise legacy adoption, malformed attempt metadata, or old status writes. |
| `execution.observation-races` | Concurrent resume starts one successor; a live observer or verifier launch cannot be parked, and a crashed observer permits explicit reinspection | manual: observer and concurrent-resume driver is not yet automated | `go/internal/cli/cutover_test.go` does not exercise concurrent resume or observer reinspection. `go/internal/execution/park_test.go` refuses a stale generation with an observer_pid set, not a live observer launch. |
| `execution.services-cleanup` | Parking during service inspection prevents launch; completed cleanup records release of the exact worker attempt | manual: service-inspection parking and cleanup-release driver is not yet automated | Implementation: `go/internal/environment/start.go`. `go/internal/cli/cutover_test.go` does not start services. |
| `execution.cleanup-races` | Cleanup excludes in-flight worker launch and competing cleanup/reconciliation; intent excludes new launches and is reconciled against fresh inspection (a matching intent continues, a stale plan re-derives) instead of wedging forever, newer attempts defeat stale proof, and conclusive refusal permits further approved work | manual: cleanup race driver is not yet automated | Implementation: `go/internal/cleanup/run.go`. `go/internal/cleanup/stop_test.go` refuses admission while stop evidence is uncertain; `go/internal/cleanup/apply_test.go` covers the intent continue and re-derive paths. |
| `execution.independent-verifier` | Root verification reserves capacity before checkout creation. Empty-boundary teardown releases. A surviving child or failed remove stays uncertain. | automated: `go/internal/cli/verify_execute_test.go` | offline suite |
| `repairs.allowance` | Only `expansion` sends consume the two-iteration allowance; in-scope sends and `execution resume` are free, expansion exhaustion saves one distinct budget decision without another prompt or launch, and in-scope sends are never blocked | automated: `go/internal/repair/repair_test.go`, `go/internal/cli/repair_test.go` | offline suite. Covers in-scope sends recording their class and charging nothing, expansion sends charging, exhaustion saving exactly one budget question, in-scope sends proceeding at exhaustion, and resume operations recording `in-scope` without consuming. Implementation: `go/internal/repair/repair.go`, `go/internal/execution/park.go`, `go/internal/cli/repair.go`, `go/internal/helpview/catalog.json`. |
| `repairs.approval` | Only explicit coordinator-recorded human grants extend the expansion allowance; repeated grants are idempotent and ordinary answers grant nothing | manual: repair grant driver is not yet automated | `go/internal/cli/repair_test.go` saves the exhaustion decision; `go/internal/cli/cutover_test.go` refuses `repair extend` without a coordinator pane. Implementation: `go/internal/repair/repair.go`. |
| `repairs.concurrent` | Shared instruction keys send once and concurrent contenders cannot exceed the last iteration | manual: concurrent repair-send driver is not yet automated | `go/internal/cli/repair_test.go` covers same-key replay returning the recorded operation and a same-key different-class refusal. Implementation: `go/internal/repair/repair.go`. |
| `repairs.persisted` | Malformed records refuse without mutation; legacy classless operations remain valid and consuming; a refused delivery rolls back with nothing recorded or charged and the same key retries; an uncertain delivery stays recorded and is not replayed; candidate, callback, refresh, saved harness, update, and rollback changes retain accounting | automated: `go/internal/repair/repair_test.go`, `go/internal/cli/repair_test.go` | offline suite covers malformed-ledger refusal, legacy classless operations, refusal rollback, and uncertain stays recorded. Candidate/refresh/update retention is not yet automated. Implementation: `go/internal/repair/repair.go`. |
| `proc.bounded-helper` | An owned helper command (git, gh, herdr, codegraph, ps, lsof) finishes within the tighter of its caller's deadline and its own timeout: multi-megabyte or never-ending output stays within the capture bound with explicit truncation flags; overflowed or incomplete JSON never becomes a Herdr or GitHub observation, and a truncated process table never reads as "nothing running"; a timeout, cancel, or descendant still holding the output returns an unknown-effect error with code -1 (never success; the held case still names the helper's own exit code in the message; the descendant is never signaled), and the returns pump keeps such a prompt `uncertain` instead of replaying it; a missing executable is a not-started refusal; `check=false` keeps nonzero exits as results; and every remaining direct process start is listed with its reason | automated: `go/internal/proc/proc_test.go`, `go/internal/proc/inventory_test.go`, `go/internal/herdrclient/herdrclient_test.go`, `go/internal/mesh/runner_test.go`, `go/internal/prcmd/pr_test.go`, `go/internal/environment/write_test.go`, `go/internal/returns/pump_test.go`, `go/internal/updatecmd/sync_test.go`, `go/internal/quota/quota_test.go` | offline suite against fake helpers. Interactive harness launches, project verification, lint, and the evidence publisher keep their own lifecycle owners. Implementation: `go/internal/proc/proc.go`. |
| `projects.enroll` | A named repository is enrolled once under `projects/<owner>/<repo>` and dispatched by name | automated: `go/internal/cli/project_test.go` | offline suite |
| `live.herdr-smoke` | A named lab Herdr session runs a real dispatch, question, and refresh | manual: `mise run test-live` on a host with Herdr 0.9.0 | operator report |
| `live.pane-incarnation` | Against the pinned real Herdr in its own `sum-test-207-*` session: repeated `init` is `same`, a live handoff (`server.live_handoff`) keeps the pane ID and shell and issues a new `terminal_id` (`handoff`, still coordinator), and a restart of that lab server restores the pane ID with a new terminal and shell (`replaced`, developer), after which `--reclaim` rebinds it | manual: `python3 scripts/live_incarnation.py` (also run first by `mise run test-live`) on a host with Herdr 0.9.0 | operator report. `restored` is not exercised live: no authenticated harness reports a native session in the lab. |
| `live.harness-canary` | An authenticated harness worker reads its brief and calls `sumctl ask` and `sumctl report` | manual: `docs/ACCEPTANCE.md` section 2 | operator report |
| `performance.hot-path-baseline` | Opt-in local metrics and `mise run benchmark` measure production helper entrypoints in fake and isolated real-Herdr labs before any rewrite decision | automated: `tests/test_benchmark.py` | `lib/sum_measure.py`, `scripts/benchmark.py`, `scripts/benchmark_fixture.py`, `scripts/benchmark_real.py`, `scripts/benchmark_report.py`, `mise-tasks/benchmark`, and retained `benchmarks/issue-37/raw.json`, `benchmarks/issue-37/report.md`, `benchmarks/issue-37/real-herdr.log` |
| `performance.coordination-pass` | The Go executable's fast coordination commands (`init`, `inbox --live`, `pump`, `status`) are measured over 1, 12, and 100 synthesized tasks (mixed active and archived) with healthy fakes and with a hung Herdr observation and slow `gh`: p50/p95 wall time, task reads, subprocesses, Herdr calls, and `gh` calls, candidate against a base build, recorded separately from the historical Python benchmark | manual: `go/internal/cli/coordination_measure_test.go` runs only with `SUM_MEASURE_OUT` set (optionally `SUM_MEASURE_BINS=base=PATH`) and needs `strace` for read and subprocess counts; the ordinary suite skips it | Retained `benchmarks/issue-206/report.md` and `benchmarks/issue-206/raw.json`. The fakes' slow knobs are `FAKE_OBSERVE_DELAY` in `tests/fixtures/herdr.py` and the `delay` scenario key in `tests/fixtures/gh.py`. |
| `performance.go-cobra-prototype` | A cgo-free compiled Cobra entrypoint is the live `bin/sumctl` helper | automated: `go/internal/cli/root_test.go`, `go/cmd/sumctl/main_test.go` | `go/go.mod`, `go/go.sum`, `go/internal/cli/root.go`, `go/cmd/sumctl/main.go` |
| `packaging.native-companion` | Setup and immutable release staging build cgo-free native binaries, install Go Mesh as the live `bin/herdr-mesh` server, record dependency provenance, and reuse installed artifacts without replacing them | automated: `tests/test_packaging.py`, `go/internal/mesh/service_test.go`, `go/cmd/herdr-mesh/main_test.go` | `docs/dependency-inventory.json`, `docs/DEPENDENCIES.md`, `docs/go-mesh.md`, `scripts/setup.py`, `scripts/install_runtime.py`, `mise.toml`, `bin/herdr-mesh`, `bin/sumctl`, `go/cmd/herdr-mesh/main.go`, `go/cmd/sumctl/main.go`, `go/internal/mesh/`, and the staged release manifest |
| `mesh.session-identity` | The Mesh MCP server and `sumctl` identify the pane's Herdr session through one shared lookup, so a pane where `sumctl` resolves a session can never leave the server refusing to start | automated: `go/internal/mesh/config_test.go` | offline suite. Covers a pane carrying no session variables, an explicit `SUM_SESSION`, a `HERDR_SESSION` fallback, a session named by the Herdr socket path, a malformed session name, and the separate outside-a-pane refusal. Implementation: `go/internal/mesh/config.go`, `go/internal/store/context.go`. |
| `mesh.machine-identity` | The Mesh bridge authorizes a pane through the same stable machine identity and registration key as `sumctl`: it finds a registration still keyed by this host's legacy hostname, and it refuses one recorded under another host's name | automated: `go/internal/mesh/service_test.go` | offline suite. Implementation: `go/internal/mesh/auth.go`, `go/internal/machine/machine.go`, `go/internal/store/store.go`. |
| `mesh.pane-incarnation` | The Mesh MCP server and the `sumctl herdr` bridge let a pane whose occupant is not the one its registration's role was granted to observe, but not prompt, send keys, or start agents: a mutating call first judges the calling pane against the owner record (coordinator) or its own registration (worker) | automated: `go/internal/mesh/service_test.go`, `go/internal/cli/incarnation_test.go` | offline suite. Implementation: `go/internal/mesh/auth.go`, `go/internal/herdrbridge/herdrbridge.go`. |
| `update.prior-helper-overlay` | A helper from before the Go Mesh cutover can copy `patches/herdr-mesh` when it stages this tree, smoke launches `bin/herdr-mesh`, the wrapper execs `herdr-mesh-go` when that is the staged name, the inventory still lists that artifact id, and smoke rewrites in-tree remainder links to relative paths | automated: `tests/test_packaging.py`, `tests/test_setup.py` | `patches/herdr-mesh/`, `bin/herdr-mesh`, `scripts/mcp_smoke.mjs`, `scripts/relink_runtime_links.mjs`, `docs/dependency-inventory.json` |
| `skills.namespace` | Sum-owned skills use one canonical `sum-*` identity across source directories, frontmatter, projections, generated references, and the immutable release worker-skill path; leftover unprefixed aliases under `skills/` are refused. The `sum-*` set stays the one every installed helper accepts, because `update apply` stages with the installed helper and its inventory refuses an unknown `sum-*` skill | automated: `go/internal/skills/skills_test.go`, `go/internal/brief/brief_test.go`, `go/internal/skilltest/bootstrap_test.go` | offline suite. Implementation: `go/internal/skills/skills.go`. |
| `grok-bot.static` | Source-controlled Grok Bot recipes under `grok-bots/sum/` (coordinator) and `grok-bots/square/` (steward) encode the operating contracts without helper commands, forbid coordinator-chat work, and CONTRIBUTING requires a Grok Bot impact check | automated: `tests/test_operating_files.py` | offline suite |
| `roles.coordinator-dispatch-only` | The bootstrap, the coordinator core, and Grok Bot instructions forbid research, planning, investigation, and implementation in the coordinator chat; those requests are dispatched | automated: `tests/test_operating_files.py` | offline suite |
| `roles.terminology` | First-party operating files and generated brief/contract samples use the Group 1 dictionary; a content check rejects themed role titles | automated: `tests/test_operating_files.py` | offline suite covers operating files and templates. Generated brief and contract samples are not yet asserted. Implementation: `go/internal/contextview/contextview.go`, `docs/terminology.md`. |
| `live.grok-bot-canary` | An operator tells a Grok Bot to follow `GROK_SUM.md`, which clones public sum and loads `grok-bots/sum/`, then dispatches one approved task to a worker Bot, questions and answers by IDs, and runs distinct worker then coordinator verification. `GROK_SQUARE.md` loads `grok-bots/square/` after Sum exists. | manual: `docs/ACCEPTANCE.md` section 13 | operator report |
| `output.toon` | Agent-facing sumctl stdout is TOON. `--format json` keeps JSON for jq and CI. On-disk records stay JSON. | automated: `go/internal/ordjson/ordjson_test.go`, `go/internal/cli` | offline suite |
| `quota.codex-remainder` | Codex quota uses Remainder `--format toon` through `sumctl quota --provider codex`. Other providers keep quota-axi. Missing Remainder refuses without falling back. | automated: `go/internal/quota/quota_test.go` | offline suite |
| `live.codex-quota-canary` | An authenticated Codex account reports remaining, exhausted, or unavailable through Remainder | manual: `docs/ACCEPTANCE.md` section 14 | operator report |
| `live.remote-machine-canary` | A real SSH machine added with Herdr `machine add` does not block local returns. Sum does not enroll the host. | manual: `docs/ACCEPTANCE.md` section 15 | operator report |
