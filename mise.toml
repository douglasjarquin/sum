[tools]
go = "1.25.0"
python = "3.13.5"
node = "22.20.0"
uv = "0.12.13"
"github:cli/cli" = "2.100.0"
"github:herdrdev/herdr" = "0.9.0"
"npm:quota-axi" = "0.1.37"
"npm:@colbymchenry/codegraph" = "1.5.0"
"npm:skills" = "1.5.25"
"pipx:basedpyright" = { version = "1.40.1", depends = ["python", "uv"] }
"go:golang.org/x/tools/gopls" = { version = "0.23.0", depends = ["go"] }

[env]
# Repository-local binaries only; never replace the system's Unix `sum` utility.
_.path = ["{{config_root}}/.local/bin"]
